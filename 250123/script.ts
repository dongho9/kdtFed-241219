// 보통함수
function add1(a: number, b: number): number {
  return a + b;
}
// // 고차함수
// const add2 =
//   (a: number): ((number: number) => number) =>
//   (b: number) =>
//     a + b;

// const result = add2(1)(2);
